{
  "name": "fastify-redis-sql-api",
  "version": "1.0.0",
  "description": "API usando Fastify, Redis e SQL",
  "main": "src/server.ts",
  "scripts": {
    "build": "tsc",
    "dev": "ts-node-dev --respawn --transpile-only src/server.ts",
    "start": "node dist/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "dotenv": "^10.0.0",
    "fastify": "^5.0.0",
    "ioredis": "^4.0.0",
    "pg": "^8.0.0",
    "reflect-metadata": "^0.1.13",
    "typeorm": "^0.3.20"
  },
  "devDependencies": {
    "ts-node-dev": "^1.1.8",
    "typescript": "^4.9.5"
  }
}
